[Unit]
Description=Timer for OSIA GDELT Global Events Ingestion — runs daily
Requires=osia-ingest-gdelt.service

[Timer]
# Daily at 06:00 UTC — staggered 30min after ACLED
OnCalendar=*-*-* 06:00:00
AccuracySec=10min
RandomizedDelaySec=1h
Persistent=true

[Install]
WantedBy=timers.target
