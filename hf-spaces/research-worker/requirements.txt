# Pinned exact versions for research_batch.py (HuggingFace Jobs).
# Exact pins mean a compromised upstream release can be compared against
# what was actually deployed. Update intentionally via PR.
httpx==0.28.1
huggingface-hub==1.12.0
